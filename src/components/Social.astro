---
const { platform, username } = Astro.props;
---

<style>
.social {
    border: 1px solid blue;
}
.social a {
    display: flex;
    align-items: center;
    justify-content: center;
    margin: 0px 3px;
}
.social a img {
    width: 19px;
}
</style>

<a href={`https://www.${platform}.com/${username}`}><img src={`/${platform}.svg`}/></a>
