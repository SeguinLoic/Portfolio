---
import '../styles/global.css';
import BaseLayout from '../layouts/BaseLayout.astro';
import TitleSection from '../components/TitleSection.astro';
import ShortBlogPost from '../components/ShortBlogPost.astro';

const pageTitle = "Home page";
const allPosts = Object.values(import.meta.glob('./posts/*.md', { eager: true }));
---

<BaseLayout pageTitle={pageTitle}>
  <TitleSection title={"Latest projects"} />
  <TitleSection title={"My thougts"} />
  <ul>
      {allPosts.map((post: any) => <ShortBlogPost url={post.url} title={post.frontmatter.title} date={post.frontmatter.pubDate} />)}
  </ul>
</BaseLayout>